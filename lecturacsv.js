//Utilización de paquete csv2geojson para convertir csv a geojson


/*
var csv2geojson = require('csv2geojson');
var geoJson = csv2geojson.csv2geojson(csvString, function(err, data) {
    // err has any parsing errors
    // data is the data.
});


csv2geojson.csv2geojson(csvString, {
    latfield: 'Latitud',
    lonfield: 'Longitud',
    delimiter: ';'
}, function(err, data) {
});


csv2geojson.csv2geojson(textFile('./Datos/Equipos.csv'), function (err, data) {
    expect(data).to.eql(jsonFile('Equipos.geojson'))
})



//function textFile(f) {
//    return fs.readFileSync('Datos/Equipos.csv' + f, 'utf8');
//}

*/
// Fuente: https://bl.ocks.org/wboykinm/9372267 //

